import { Meta, StoryObj } from "@storybook/react";
import { Cards, CardsProps } from ".";

export default {
  title: "Cards/Card",
  component: Cards,
} as Meta<CardsProps>;

export const Default: StoryObj<CardsProps> = {};
